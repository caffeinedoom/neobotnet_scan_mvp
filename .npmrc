# Enable pre/post install scripts for specific packages
# This is required for packages that need to compile native code or download binaries
enable-pre-post-scripts=true

# Alternative: If you want more granular control, you can specify individual packages
# But enabling globally for trusted dependencies is fine for most projects
